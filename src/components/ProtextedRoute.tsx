import React from "react";
type ProtectedRouteProps = {
  children: React.ReactNode;
};

const ProtextedRoute = ({ children }: ProtectedRouteProps) => {
  return <div></div>;
};

export default ProtextedRoute;
